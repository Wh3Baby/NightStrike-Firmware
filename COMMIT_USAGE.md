# 📝 Использование скриптов коммита

## commit.sh - Интерактивный коммит

**Базовое использование:**
```bash
./commit.sh
```
Скрипт запросит сообщение коммита интерактивно.

**С сообщением коммита:**
```bash
./commit.sh "Описание изменений"
```

**Что делает:**
1. ✅ Проверяет статус репозитория
2. ✅ Показывает изменения
3. ✅ Добавляет все файлы
4. ✅ Создает коммит
5. ✅ Отправляет на GitHub

---

## commit_auto.sh - Автоматический коммит

**Использование:**
```bash
./commit_auto.sh
```

**С кастомным сообщением:**
```bash
./commit_auto.sh "Мое сообщение коммита"
```

**Что делает:**
1. ✅ Автоматически определяет тип изменений:
   - `feat(module)` - изменения в модулях
   - `refactor(core)` - изменения в core
   - `docs(README)` - изменения в документации
   - `build(config)` - изменения в конфигурации
   - `feat(boards)` - изменения в конфигурациях плат
2. ✅ Добавляет timestamp
3. ✅ Создает коммит
4. ✅ Отправляет на GitHub

---

## Примеры использования

**Обычный коммит:**
```bash
./commit.sh "Добавлена поддержка новой платы"
```

**Автоматический коммит:**
```bash
./commit_auto.sh
# Создаст: "feat(wifi): автоматический коммит (2026-01-02 20:00:00)"
```

**Быстрый коммит с автоопределением:**
```bash
./commit_auto.sh "Исправлена ошибка в WiFi модуле"
```

---

## Рекомендации

- Используйте `commit.sh` для важных изменений с детальным описанием
- Используйте `commit_auto.sh` для быстрых коммитов и мелких правок
- Всегда проверяйте изменения перед коммитом: `git status`
- Используйте понятные сообщения коммитов

---

**Примечание:** Оба скрипта автоматически отправляют изменения на GitHub после создания коммита.
