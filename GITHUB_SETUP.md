# 🌑 Настройка GitHub репозитория

## Важно: 2FA на аккаунте

Для работы с GitHub при включенной 2FA нужен **Personal Access Token**, а не пароль.

### Создание Personal Access Token

1. Перейдите на: https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Название: `NightStrike-Firmware`
4. Срок действия: выберите нужный (рекомендуется: 90 дней или No expiration)
5. Права (scopes):
   - ✅ `repo` (полный доступ к репозиториям)
6. Нажмите "Generate token"
7. **Скопируйте токен** (он показывается только один раз!)

### Создание приватного репозитория

**Вариант 1: Через веб-интерфейс (рекомендуется)**

1. Перейдите на: https://github.com/new
2. Repository name: `NightStrike-Firmware`
3. Description: `Advanced ESP32 Firmware for Offensive Security Operations`
4. Выберите: **Private** ✅
5. **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
6. Нажмите "Create repository"

**Вариант 2: Через GitHub CLI**

```bash
# Авторизация
gh auth login

# Создание приватного репозитория
gh repo create NightStrike-Firmware --private --source=. --remote=origin --push
```

### Подключение локального репозитория

После создания репозитория на GitHub:

```bash
# Добавить remote
git remote add origin https://github.com/wh3baby/NightStrike-Firmware.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код
git push -u origin main
```

**При запросе пароля используйте Personal Access Token вместо пароля!**

### Настройка Git Credential Helper (для удобства)

```bash
# Кэшировать credentials на 1 час
git config --global credential.helper 'cache --timeout=3600'

# Или сохранить в файл (менее безопасно)
git config --global credential.helper store
```

### Проверка подключения

```bash
git remote -v
git log --oneline
```

---

**Готово!** Репозиторий настроен и готов к работе.
